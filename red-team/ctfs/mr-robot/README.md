# Mr Robot (TryHackMe) — CTF Repo

Pro workflow, clean history, reproducible steps.

## Structure
- `docs/` — walkthrough (spoiler‑light), references
- `scans/` — nmap outputs, service notes
- `exploits/` — POCs, payloads
- `scripts/` — helper scripts (enum/brute/automation)
- `notes/` — scratchpad + commands for Joplin import
- `loot/` — creds/flags (gitignored)

## How to run
See `docs/walkthrough.md` for the step plan.

